package image

/*
   this package implements the virtual machine image building. By default this daemon can be provided
   with a GitURL to build a new VM image, which is then uploaded through metadata service API.
*/
